{
  "name": "key-intercept-selfhosted-plugin",
  "version": "0.1.0",
  "description": "Kettu plugin that reads local config and remotely edits allowed users via relay",
  "main": "keyInterceptSelfHosted.tsx",
  "type": "module",
  "scripts": {
    "test": "node ./test/smoke.test.mjs"
  },
  "license": "MIT"
}
